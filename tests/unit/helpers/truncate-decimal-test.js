
import { truncateDecimal } from 'ui/helpers/truncate-decimal';
import { module, test } from 'qunit';

module('Unit | Helper | truncate decimal');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = truncateDecimal([42]);
  assert.ok(result);
});

