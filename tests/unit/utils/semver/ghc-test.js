import semverGhc from 'ui/utils/semver/ghc';
import { module, test } from 'qunit';

module('Unit | Utility | semver/ghc');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = semverGhc();
  assert.ok(result);
});
