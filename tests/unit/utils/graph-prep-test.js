import { toDataPoints } from 'ui/utils/graph-prep';
import { module, test } from 'qunit';

module('Unit | Utility | graph-prep');

// TODO test later.
test('it works', function(assert) {
  assert.ok(toDataPoints);
});
