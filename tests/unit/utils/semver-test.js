import semver from 'ui/utils/semver';
import { module, test } from 'qunit';

module('Unit | Utility | semver');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = semver();
  assert.ok(result);
});
