
{{yield (hash
  controls=(component 'layout/-graph-controls'
    config=_controlConfig
    class=classes.panel)
  graphBody=(component 'layout/-graph-page/body'
    selectedBounds=_selectedBounds
    currentScript=currentScript
    filtered=_filtered
    class=classes.body)
)}}


